# Pipeline run artifacts (regenerated each run)
cleanup_pages_done_*.json
cleanup_pages.json
email_body.txt
telegram_msg.txt
jobs_rows.json

# Discovery & verification batch files (intermediate pipeline data)
discovery_*.json
verify_batch_*.json
verified_candidates.json

# Raw CSV exports (may contain passwords/credentials)
*.csv

# Environment / secrets
.env
*.env

# OS files
.DS_Store
Thumbs.db

# Claude Code local settings (user-specific, not shared)
.claude/*
# But DO track project-level skills (portable across machines)
!.claude/skills/
